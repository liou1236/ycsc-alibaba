#所有的用户(权限)服务使用807x(8071/8072/8073)
spring:
  #配置服务名称
  application:
    name: shop-user

    #配置nacos服务器所在地址（默认本机IP端口为8848）
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        #配置文件扩展名
        file-extension: yaml

  profiles:
    #区分环境（dev）
    active: dev  #环境标识

